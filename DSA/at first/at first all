// #include<iostream>

// using namespace std;

// class Node{
//     // Data Members:
//     public:
//         int value;
//         Node *next;
        
//         // Constructor....
//         Node(int value){
//             this->value = value;
//             this->next = nullptr;
//         }
// };

// // According-> "NULL" got depricated and from now onwards use only "nullptr" applicable to mentor as well....
 
// class SinglyLinkedList{
    
//     private:
//       Node *head;
      
     
//     public:
//       SinglyLinkedList(){
//           this->head = nullptr;
//       }
      
//     //   Function Members:
//       void insertAtFirst(int data){
//         //   Create new Node first with the help of Node class which is made just above this class....
//           Node *temp = new Node(data);
//         //   Tackle 2-case/Scenerio for this method:
//           temp->next = head;
//           head = temp;
//       }
    
//       void display(){
//           if(head == nullptr){
//               cout << "bhaag yaha se"<<endl;
//               return;
//           }
          
//           Node  *temp = head;
//           while(temp != nullptr){
//               cout << "The current node value is -> " << temp->value << "-> " << endl; 
//               temp = temp->next;
//           }
//       }
// };


// int main(){
    
//     SinglyLinkedList obj1;
//     obj1.insertAtFirst(23);
//     obj1.insertAtFirst(56);
//     obj1.insertAtFirst(47);
//     obj1.display();
//     cout << "The node is created "<< endl;
    
    
    
//     return 0;
// }






#include <iostream>
using namespace std;

// Node Class
class Node {
public:
    int data;
    Node* next;

    Node(int value) {
        data = value;
        next = nullptr;
    }
};

// Singly Linked List Class
class SinglyLinkedList {
private:
    Node* head;

public:
    SinglyLinkedList() {
        head = nullptr;
    }

    // Insert at First
    void insertAtFirst(int value) {
        Node* newNode = new Node(value);
        newNode->next = head;
        head = newNode;
    }

    // Insert at Last
    void insertAtLast(int value) {
        Node* newNode = new Node(value);

        if (head == nullptr) {
            head = newNode;
            return;
        }

        Node* temp = head;

        while (temp->next != nullptr) {
            temp = temp->next;
        }

        temp->next = newNode;
    }

    // Insert at Position
    void insertAtPosition(int pos, int value) {
        if (pos == 1) {
            insertAtFirst(value);
            return;
        }

        Node* newNode = new Node(value);
        Node* temp = head;

        for (int i = 1; i < pos - 1 && temp != nullptr; i++) {
            temp = temp->next;
        }

        if (temp == nullptr) {
            cout << "Invalid Position\n";
            return;
        }

        newNode->next = temp->next;
        temp->next = newNode;
    }

    // Delete at First
    void deleteAtFirst() {
        if (head == nullptr) {
            cout << "List is Empty\n";
            return;
        }

        Node* temp = head;
        head = head->next;
        delete temp;
    }

    // Delete at Last
    void deleteAtLast() {
        if (head == nullptr) {
            cout << "List is Empty\n";
            return;
        }

        if (head->next == nullptr) {
            delete head;
            head = nullptr;
            return;
        }

        Node* temp = head;

        while (temp->next->next != nullptr) {
            temp = temp->next;
        }

        delete temp->next;
        temp->next = nullptr;
    }

    // Display
    void display() {
        if (head == nullptr) {
            cout << "List is Empty\n";
            return;
        }

        Node* temp = head;

        while (temp != nullptr) {
            cout << temp->data << " -> ";
            temp = temp->next;
        }

        cout << "NULL" << endl;
    }
};

// Main Function
int main() {
    SinglyLinkedList list;

    list.insertAtFirst(20);
    list.insertAtFirst(10);

    list.insertAtLast(30);
    list.insertAtLast(40);

    cout << "After Insertions:\n";
    list.display();

    list.insertAtPosition(3, 25);

    cout << "\nAfter Insert at Position:\n";
    list.display();

    list.deleteAtFirst();

    cout << "\nAfter Delete at First:\n";
    list.display();

    list.deleteAtLast();

    cout << "\nAfter Delete at Last:\n";
    list.display();

    return 0;
}


// code is best but good preactice